# Database - Using in-memory for demo
DATABASE_URL=sqlite::memory:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=performix_db
DB_USER=demo_user
DB_PASSWORD=demo_password

# JWT
JWT_SECRET=performix-demo-jwt-secret-key-2024
JWT_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=development

# Stripe - Demo keys
STRIPE_SECRET_KEY=sk_test_demo_key
STRIPE_PUBLISHABLE_KEY=pk_test_demo_key
STRIPE_WEBHOOK_SECRET=whsec_demo_secret

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=demo@performix.com
SMTP_PASS=demo_password

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=uploads/
